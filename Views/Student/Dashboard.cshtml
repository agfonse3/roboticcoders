@model List<StudentCourseViewModel>

<h2>🎓 Mis Cursos</h2>

@foreach (var course in Model)
{
    <div style="border:1px solid #ccc; padding:15px; margin-bottom:15px;">
        <h3>@course.Title</h3>
        <p>@course.Description</p>

        <div style="background:#eee; height:20px; border-radius:5px;">
            <div style="
                width:@course.ProgressPercent%;
                background:#4caf50;
                height:20px;
                border-radius:5px;">
            </div>
        </div>

        <p>@course.ProgressPercent% completado</p>

        <a href="/Student/Course/@course.CourseId">
            Continuar →
        </a>
    </div>
}
