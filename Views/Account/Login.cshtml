@{
    ViewData["Title"] = "Iniciar Sesión";
}

<div class="login-page">

    <div class="login-card">

        <!-- LADO IZQUIERDO -->
        <div class="login-info">
            <h2>Bienvenido a RoboticCoders 🚀</h2>
            <p>
                Aprende programación, robótica e idiomas
                de forma divertida y práctica.
            </p>

            <ul>
                <li>🎮 Cursos interactivos</li>
                <li>🤖 Robótica y tecnología</li>
                <li>🌍 Idiomas para el futuro</li>
            </ul>
        </div>

        <!-- FORMULARIO -->
      <div class="login-form">

    <h3>Iniciar sesión</h3>
    <p class="login-subtitle">
        Accede a tu panel de aprendizaje
    </p>

    @if (ViewBag.Error != null)
    {
        <div class="login-error">
            @ViewBag.Error
        </div>
    }

    <form asp-controller="Account" asp-action="Login" method="post">

        <input type="hidden" name="returnUrl" value="@ViewData["ReturnUrl"]" />

        <div class="form-group">
            <label>Correo electrónico</label>
            <input type="email" name="email" placeholder="ejemplo@roboticcoders.com" required />
        </div>

        <div class="form-group">
            <label>Contraseña</label>
            <input type="password" name="password" placeholder="••••••••" required />
        </div>

        <button type="submit" class="btn-primary login-btn">
            Entrar al sistema
        </button>
    </form>

</div>

    </div>

</div>